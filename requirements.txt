# Core Web Framework
streamlit>=1.29.0,<2.0.0
extra-streamlit-components>=0.1.60,<1.0.0
streamlit-extras>=0.3.6,<1.0.0
streamlit-aggrid>=0.3.4,<1.0.0

# Data Processing & Visualization
plotly>=5.17.0,<6.0.0
# Hover/click event roundtrip for plotly charts (B11 cluster heatmap).
# Pinned because the lib has no maintained release schedule; the click
# fallback (st.plotly_chart on_select="rerun") works on Streamlit 1.41+
# if the lib breaks on a future Streamlit upgrade.
streamlit-plotly-events>=0.0.6,<0.1.0
pandas>=2.0.0,<3.0.0
numpy>=1.24.0,<2.0.0
matplotlib>=3.7.0,<4.0.0
seaborn>=0.12.0,<1.0.0

# Scientific Computing
scipy>=1.10.0,<2.0.0
scikit-learn>=1.3.0,<2.0.0
mdtraj>=1.9.8,<2.0.0

# Molecular Visualization
# py3Dmol still needed by cluster_visualization.show_pocket_3d (the
# in-panel cluster-preview viewer); the rest of the suite renders
# through the Mol* component.
py3Dmol>=2.0.4,<3.0.0

# Molecular Docking (smina + openbabel + prody) and structure handling
# (prody) install via conda-forge in the Dockerfile — see the
# micromamba section there. Keeping them out of pip avoids the
# openbabel C++ ABI mismatch the host bind-mount used to cause.

# Task Queue & Message Broker
celery>=5.3.0,<6.0.0
redis>=4.6.0,<5.0.0

# Persistence (v2 Phase A)
sqlalchemy>=2.0,<3.0
alembic>=1.13,<2.0
psycopg[binary]>=3.1,<4.0
pydantic-settings>=2.0,<3.0

# Trajectory → viewer-format conversion (v2 Phase B B2)
# gemmi reads per-frame PDBs and writes the multi-model mmCIF Mol* loads.
# (BCIF write isn't yet exposed in gemmi's Python bindings — text mmCIF
# until that lands; output file is the same shape Mol* expects.)
gemmi>=0.6.5,<1.0

# Utilities
python-dotenv>=1.0.0,<2.0.0
tqdm>=4.65.0,<5.0.0
ipython>=8.0.0,<9.0.0
# B3.3: MIME sniffing on uploaded files (defence-in-depth above the
# extension allowlist). Needs libmagic1 in the system layer of the image.
python-magic>=0.4.27,<1.0

# Optional (Jupyter Integration)
ipywidgets>=8.0.0,<9.0.0 